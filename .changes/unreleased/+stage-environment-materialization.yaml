kind: Changed
body: Make `stage` and `stage --update` record desired environment state without building; let staged up and restart build on demand; and add `stage --update APP_REF --force` to replace staging owned by another blueprint after stopping its active work.
