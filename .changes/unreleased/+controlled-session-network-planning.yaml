kind: Added
body: Freeze granted controlled-session workload endpoints and lease-private network identities into immutable plans, with protocol-v2 session-local coordinates that remain fail-closed until the planned network is realized.
