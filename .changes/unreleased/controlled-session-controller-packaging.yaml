kind: Added
body: Embed matching Linux controller executables in Reploy releases and prepare a release-pinned, controller-only image before controlled-session admission without exposing the tool to the workload image.
