kind: Fixed
body: Create controlled-session Docker networks with collision-checked explicit subnets drawn from the daemon's address-pool policy so their fixed controller and workload addresses work consistently across supported Docker runtimes.
