[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "smoke-imap"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["smoke-suite==0.1.0"]

[tool.setuptools.packages.find]
where = ["src"]
